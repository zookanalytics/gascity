// Package event defines event types and audit records for hosted operations.
// It extends the internal event vocabulary with workspace, session, bundle,
// checkpoint, and operation lifecycle events.
//
// This package is pure types and constants — no I/O, no dependencies on
// internal packages.
package event
